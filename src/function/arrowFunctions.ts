//Normal Functions
let log = function (message) {
  console.log(message);
};

//Arrow Functions
let arrowLog = (message) => {
  console.log(message);
};

//If there is only one line inside function we dont need `{,}`
let oneLineArrowFunc = (message) => console.log(message);
